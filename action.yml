# Copyright IBM Corp. 2023
# Made available under the MIT License

name: "Login to StepZen Server"
author: "IBM"
description: |
  Log into StepZen Server

inputs:
  domain:
    description: "StepZen domain (defaults to stepzen.net)"
    required: true
    default: "stepzen.net"
  account:
    description: "StepZen account name"
    required: false
  adminkey:
    description: "Admin key of the StepZen account OR API Connect for GraphQL instance's API key"
    required: true
  instanceid:
    description: "API Connect for GraphQL instance identifier"
    required: false
  max_attempts:
    description: "Number of retry attempts."
    required: false
    default: "3"
  retry_wait_seconds:
    description: "Time to wait between retries in seconds."
    required: false
    default: "10"
  timeout_seconds:
    description: "Seconds to wait before attempt times out"
    required: false
    default: "60"
  

outputs:
  domain:
    description: "Domain the account is logged into."
    value: ${{ steps.output.outputs.domain }}
  account:
    description: "Account that is logged in."
    value: ${{ steps.output.outputs.account }}
  apikey:
    description: "API key for the account."
    value: ${{ steps.output.outputs.apikey }}
  instanceid: 
    description: "API Connect for GraphQL instance identifier"
    value: ${{ steps.output.outputs.instanceid }}
  token:
    description: "Access token"
    value: ${{ steps.output.outputs.token }}

runs:
  using: "composite"
  steps:
    # Don't assume the admin key came from a secret so mask it for safety
    - name: mask
      shell: bash
      run: |
        adminkey=${{ inputs.adminkey }}
        echo "::add-mask::${adminkey}"
    - name: "login"
      uses: nick-fields/retry@v3
      with:
        timeout_seconds: ${{ inputs.timeout_seconds }}
        max_attempts: ${{ inputs.max_attempts }}
        retry_wait_seconds: ${{ inputs.retry_wait_seconds }}
        shell: bash
        command: |
          if [ "${{ inputs.instanceid }}" != "" ]; then
            stepzen login ${{ inputs.domain }} --instanceId ${{ inputs.instanceid }} --adminkey ${{ inputs.adminkey }}
          elif [ "${{ inputs.account }}" != "" ]; then
            stepzen login ${{ inputs.domain }} --account ${{ inputs.account }} --adminkey ${{ inputs.adminkey }}
          else
            echo "Not found valid account name or instance id"
            exit 1
          fi          
    - id: output
      run: |
        if [ "${{ inputs.instanceid }}" != "" ]; then
          echo "token=$(stepzen whoami --accesstoken)" >> $GITHUB_OUTPUT
          echo "instanceid=${{ inputs.instanceid }}" >> $GITHUB_OUTPUT 
        elif [ "${{ inputs.account }}" != "" ]; then
          apikey=$(stepzen whoami --apikey)
          echo "::add-mask::${apikey}"                        
        else
            echo "Not able to find the stepzen's api key or access token"
            exit 1
        fi  
        echo "account=$(stepzen whoami --account)" >> $GITHUB_OUTPUT                
        echo "domain=$(stepzen whoami --domain)" >> $GITHUB_OUTPUT        
        echo "apikey=${apikey}" >> $GITHUB_OUTPUT
      shell: bash
